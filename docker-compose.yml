version: '3.8'

services:
  video-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: magic-mirror-video-service
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
